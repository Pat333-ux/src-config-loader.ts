# src-config-loader.ts
The Beast System 3.0 Configuration Loader reads environment, system, module, port, and identity configuration files, merges them into a unified runtime config, and supplies the Activation Kernel and Runtime Shell with validated settings. It ensures consistent, environment‑aware boot behavior across all deployments.
